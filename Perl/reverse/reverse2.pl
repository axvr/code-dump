#!/usr/bin/env perl

use strict;
use warnings;

# Reverse a number using array manipulation
sub reverse {
    my $number = $_[0];

    return reverse($number);
}

my $number = 123456789123456789;

print(main::reverse($number));
